name: ansible lint

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "envs/**/*.yaml"
  pull_request:
    branches:
      - main
    paths:
      - "envs/**/*.yaml"

env:
  TERM: xterm

jobs:
  ansible-playbook:
    strategy:
      fail-fast: false
      matrix:
        os: [macos, macos_intel, raspi, ubuntu, ubuntu_wsl1]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v3
      - name: Install ansible
        run: pip3 install ansible
      - name: Install ansible-lint
        run: pip3 install ansible-lint
      - name: Run ansbile-lint
        run: ansible-lint ./envs/${{ matrix.os }}/playbook.yaml
