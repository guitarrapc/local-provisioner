- name: "git - Add ppa:git-core ppa repository"
  become: true
  apt_repository:
    repo: ppa:git-core/ppa

- name: "git - Install latest git"
  become: true
  apt:
    pkg: "git"
    update_cache: true
    force_apt_get: true
