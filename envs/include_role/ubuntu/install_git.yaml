- name: "git - Add ppa:git-core ppa repository"
  become: true
  apt_repository:
    repo: ppa:git-core/ppa
    update_cache: true

- name: "git - Install latest git"
  become: true
  apt:
    pkg: "git"
    update_cache: true
    force_apt_get: true
