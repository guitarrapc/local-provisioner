# https://github.com/lukesampson/scoop-extras
- name: "Install exstra bucket"
  scoop_bucket_install:
    state: present
    bucket: extras

- name: "Install Extra tools"
  scoop_install:
    state: present
    bucket: extras
    name:
      - azure-cli
      - azure-functions-core-tools
      - chromium
      - dnspy
      - everything
      - fiddler
      - gitkraken
      - hack-font
      - heidisql
      - kdiff3
      - linqpad
      - slack
      - vscode
